import React, { useEffect } from 'react';

const Home = (props) => {

    // useEffect((() => {
    //     if (!props.auth) {
    //         props.history.push('/login')
    //     }
    // }), [])

    return (
        <div>
            <h1>Welcome!</h1>

        </div>
    )
}

export default Home;